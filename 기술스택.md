# 프로젝트 구조 · 파일 역할 · 기술 스택

## 한눈에 보는 데이터 흐름
```
입력(영수증·승인내역·엑셀·양식)
   → [파싱] parse_receipt / parse_approval / parse_domestic / parse_master
   → [매칭] match  (+ fx 환율)
   → [출력] build_pdf(60% 표기 PDF) , gen_gianseo_hwpx + hwpx_fill(기안서)
   → 출력/<기간>/
전체 조율: run.py  /  실행 편의: run.bat·setup.bat  /  설정: config.json·환율.json
```

## 파일별 역할

### 실행·설정
| 파일 | 역할 |
|------|------|
| **run.py** | 진입점. 입력을 읽어 파싱→매칭→출력까지 전체 파이프라인을 조율 |
| **config.json** | 설정값(입력 폴더, 카드번호 6571/8043, 온라인 환율 사용 여부) |
| **환율.json** | (선택) 날짜별 환율 직접지정. 비어 있으면 인터넷 자동조회 |
| **requirements.txt** | 설치할 파이썬 패키지 목록 |
| **setup.bat** | (윈도우) 최초 설치 자동화 — 가상환경 생성 + 패키지 설치 |
| **run.bat** | (윈도우) 매달 실행 — 더블클릭하면 run.py 실행 |
| **백업.bat** | 소스코드(.py·.json·.md·.bat) 날짜폴더에 복사 백업 |
| **README.md** | 설치·실행 사용설명서 |

### 입력 읽기(파서)
| 파일 | 역할 |
|------|------|
| **parse_receipt.py** | 영수증 파서(PDF·HTML만). Stripe(OpenAI/Anthropic)·Google Cloud·Google Play·범용폴백 4유형. **중복 자동 제거** |
| **parse_approval.py** | 해외 우리카드 '해외카드 매출내역' PDF 파서 (USD·원화청구·환불 구분) |
| **parse_domestic.py** | 국내 우리BC카드 승인내역 PDF 파서 (건당 1페이지, 구글클라우드/구글플레이) |
| **parse_master.py** | 마스터 엑셀(2026 AI.xlsx) 로더. 이메일/이름→부서·담당자 매핑, 사용자표기 규칙 |

### 처리
| 파일 | 역할 |
|------|------|
| **match.py** | 매칭 엔진. 영수증↔승인내역을 카드+가맹점+금액+날짜로 1:1 매칭, 사용자 결합, 미매칭 표시 |
| **fx.py** | 환율 조회. 당일 환율을 인터넷에서 자동조회(+직접지정 오버라이드/실패 처리) |

### 출력 생성
| 파일 | 역할 |
|------|------|
| **build_pdf.py** | 60% 축소 + 표기 PDF. 영수증 빈 공간(가로밴드)을 찾아 카드뒷자리·사용자·원화를 짙은 파랑 굵은 고딕으로 얹음 |
| **gen_gianseo_hwpx.py** | 기안서 대상 판별(제외리스트·Claude API 분기) + 금액($×환율) 계산 + 기안서 항목 생성 |
| **hwpx_fill.py** | 한글 .hwpx 양식의 텍스트만 교체하는 엔진 (양식·서식 100% 보존) |

## 기술 스택

### 언어
- **Python 3** — 인터프리터 언어. 터미널에서 `python run.py`로 실행. `config.json`으로 경로·옵션 설정, `.bat` 파일은 더블클릭 편의용 래퍼.

### 라이브러리
| 분야 | 라이브러리 | 용도 |
|------|-----------|------|
| PDF 읽기 | **pdfplumber** | PDF에서 텍스트·단어 위치 추출 |
| PDF 가공 | **pypdf** | 페이지 60% 축소·병합, 좌표 변환(Transformation) |
| 엑셀 | **openpyxl** | 마스터 엑셀 읽기, 셀 배경색(노랑) 감지 |
| PDF 그리기 | **reportlab** | 표기 글씨(한글 CID 폰트/나눔고딕), 가짜볼드(스트로크) |
| 한글 문서 | **zipfile + XML(정규식)** | .hwpx(OWPML) 텍스트 치환 |
| 환율 | **urllib** + open.er-api.com / exchangerate.host | 당일 USD→KRW 조회 |

### 핵심 기법(알고리즘)

**파싱·매칭**
- **다단계 파싱**: 유형별 정밀 추출(Stripe/GCloud/GPlay) → 실패 시 범용 폴백. "승인내역이 정답지" 원칙.
- **가맹점 정규화**: OPENAI / ANTHROPIC·CLAUDE / GOOGLE CLOUD / GOOGLE PLAY 4그룹으로 묶어 비교.
- **1:1 그리디 매칭**: 카드+가맹점+금액 동일 + 날짜 근접(±7일) 중 가장 가까운 것 선택, 각 승인 1회만.
- **중복 제거**: (type·vendor·date·usd·krw·card·receipt_no·order_no) 조합으로 동일 영수증 제거.

**60% 표기 PDF**
- **여백 자동 탐지**: pdfplumber 단어 위치로 빈 가로밴드(상단 12% 제외, 최대 크기) 탐지.
- **x축 위치 결정**: 빈 밴드가 페이지 상단 40% 이내면 오른쪽(x_frac=0.60), 아니면 왼쪽(x_frac=0.10). Gmail 계열 영수증 상단 로고 영역 겹침 방지.
- **표기 순서**: ① 원본 60% 축소(Transformation.scale+translate) → ② 10pt 고정크기 표기 오버레이 병합.

**기안서 생성**
- **제외 리스트**: `_EXCL_PERSONAL`(개인별 벤더 제외)·`_EXCL_DEPT`(부서별 벤더 제외) — 연간 기안 커버 계정은 개별 기안서 생략.
- **description → 구독항목**: `_desc_to_prods()` — "Max plan - 5x" → (Claude, Max, 5x), "Auto-recharge credits" → (Claude, API, credits), "ChatGPT Plus..." → (ChatGPT, Plus, "").
- **Claude API 분기**: `jongwoo4u@gmail.com` + Auto-recharge → 사용자="회사 공용 API". 30만원 미만 복수건은 묶음 기안서 1건, 30만원 이상은 개별.
- **전부 개별 기안서**: Claude API 묶음 외 모든 항목은 항목마다 별도 hwpx 파일 생성.
- **HWPX 텍스트 치환**: 양식을 그대로 두고 `<hp:t>` 요소만 인덱스로 교체 → 한글에서 깨짐 없이 열림.

### 결과물
- **영수증_60퍼_표기_<기간>.pdf** (60% + 카드뒷자리·사용자·원화, 매칭순 정렬)
- **기안서/*.hwpx** (항목별 개별 기안서, $×당일환율)
- **점검_<기간>.txt** (매칭/미매칭/기안서 요약 리포트)
